import { AddDotsToEndPipe } from './add-dots-to-end.pipe';

describe('AddDotsToEndPipe', () => {
  it('create an instance', () => {
    const pipe = new AddDotsToEndPipe();
    expect(pipe).toBeTruthy();
  });
});
