import { HighlitDirective } from './highlit.directive';

describe('HighlitDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlitDirective();
    expect(directive).toBeTruthy();
  });
});
